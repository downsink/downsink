{
  "name": "@downsink/e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "pnpm --filter @downsink/core run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
